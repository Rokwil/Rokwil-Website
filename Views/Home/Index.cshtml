@{
    ViewData["Title"] = "Home";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@* Hero section - Replace with actual Keystone Park image when available *@
<section class="hero" style="background-image: url('https://images.unsplash.com/photo-1504307651254-35680f356dfd?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80'); background-size: cover; background-position: center;">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 class="hero-title">Building Excellence in Property Development</h1>
        <p class="hero-subtitle">Transforming landscapes, creating opportunities, shaping the future of KwaZulu-Natal, as the economic gateway to Sub-Saharan Africa</p>
        <div class="hero-buttons">
            <a asp-controller="Home" asp-action="Projects" class="btn btn-primary">Our Projects</a>
            <a asp-controller="Home" asp-action="Contact" class="btn btn-secondary">Get In Touch</a>
        </div>
    </div>
    <div class="scroll-indicator">
        <span></span>
    </div>
</section>

<section class="features">
    <div class="container">
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">🏗️</div>
                <h3>Industrial Development</h3>
                <p>State-of-the-art logistics and warehousing facilities designed for modern business needs. Our 152-hectare Keystone Park development hosts major tenants including Mr Price, Pepkor, and Kerry Foods.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🏢</div>
                <h3>Commercial Spaces</h3>
                <p>Premium corporate office spaces in prime locations with exceptional design and quality. Judges Court in Upper Highway offers businesses prestigious addresses with modern facilities.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🌱</div>
                <h3>Community Impact</h3>
                <p>Committed to creating developments that generate employment opportunities. Keystone Park alone is expected to create 6,500 permanent jobs, contributing significantly to regional economic growth.</p>
            </div>
        </div>
    </div>
</section>

<section class="showcase">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Featured Projects</h2>
            <p class="section-subtitle">Discover our landmark developments</p>
        </div>
        <div class="showcase-grid">
            @* Keystone Park showcase - Replace with actual image when available *@
            <div class="showcase-item">
                <div class="showcase-image" style="background-image: url('https://images.unsplash.com/photo-1504307651254-35680f356dfd?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80'); background-size: cover; background-position: center;">
                    <div class="showcase-overlay">
                        <h3>Keystone Park</h3>
                        <p>R6.5 Billion • 152-hectare logistics and industrial precinct</p>
                        <a asp-controller="Home" asp-action="Projects" class="btn btn-outline">Learn More</a>
                    </div>
                </div>
            </div>
            <div class="showcase-item">
                <div class="showcase-image" style="background-image: url('https://images.unsplash.com/photo-1497366216548-37526070297c?ixlib=rb-4.0.3&auto=format&fit=crop&w=2069&q=80'); background-size: cover; background-position: center;">
                    <div class="showcase-overlay">
                        <h3>Judges Court</h3>
                        <p>Premium corporate office space in Upper Highway</p>
                        <a asp-controller="Home" asp-action="Projects" class="btn btn-outline">Learn More</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<section class="stats">
    <div class="container">
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-number" data-target="152">152</div>
                <div class="stat-label">Hectares Developed</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" data-target="6500">6,500</div>
                <div class="stat-label">Permanent Jobs Created</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" data-target="6.5">6.5</div>
                <div class="stat-label">Billion Rand Investment</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" data-target="7">7</div>
                <div class="stat-label">Major Tenants</div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        // Animate stats on scroll
        const observerOptions = {
            threshold: 0.5,
            rootMargin: '0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const statNumbers = entry.target.querySelectorAll('.stat-number');
                    statNumbers.forEach(stat => {
                        const target = parseFloat(stat.getAttribute('data-target'));
                        animateValue(stat, 0, target, 2000);
                    });
                    observer.unobserve(entry.target);
                }
            });
        }, observerOptions);

        const statsSection = document.querySelector('.stats');
        if (statsSection) {
            // Check if section is already visible on page load
            const checkVisibility = () => {
                const rect = statsSection.getBoundingClientRect();
                const isVisible = rect.top < window.innerHeight && rect.bottom > 0;
                if (isVisible) {
                    const statNumbers = statsSection.querySelectorAll('.stat-number');
                    statNumbers.forEach(stat => {
                        const target = parseFloat(stat.getAttribute('data-target'));
                        const currentText = stat.textContent;
                        // Only animate if current value is different from target
                        if (currentText !== target.toString() && currentText !== target.toFixed(1)) {
                            animateValue(stat, 0, target, 2000);
                        }
                    });
                }
            };
            
            // Check immediately and on scroll
            checkVisibility();
            observer.observe(statsSection);
        }

        function animateValue(element, start, end, duration) {
            let startTimestamp = null;
            const isDecimal = end % 1 !== 0; // Check if target is a decimal
            const step = (timestamp) => {
                if (!startTimestamp) startTimestamp = timestamp;
                const progress = Math.min((timestamp - startTimestamp) / duration, 1);
                let current;
                if (isDecimal) {
                    current = (progress * (end - start) + start).toFixed(1);
                } else {
                    if (end >= 1000) {
                        // Format large numbers with commas
                        current = Math.floor(progress * (end - start) + start).toLocaleString();
                    } else {
                        current = Math.floor(progress * (end - start) + start);
                    }
                }
                element.textContent = current;
                if (progress < 1) {
                    window.requestAnimationFrame(step);
                } else {
                    if (isDecimal) {
                        element.textContent = end.toFixed(1);
                    } else if (end >= 1000) {
                        element.textContent = end.toLocaleString();
                    } else {
                        element.textContent = end;
                    }
                }
            };
            window.requestAnimationFrame(step);
        }
    </script>
}

